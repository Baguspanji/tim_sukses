<%- contentFor('body') %>

    <div class="row">
        <!-- left column -->
        <div class="col-md-12">
            <!-- jquery validation -->
            <div class="card card-lightblue">
                <div class="card-header">
                    <h3 class="card-title">
                        <%= title %>
                    </h3>
                </div>
                <!-- /.card-header -->
                <!-- form start -->
                <form class="needs-validation" action="<%= title.split(' ')[0] == 'Tambah' ? '/user' : '/user/' + data.id_user %>" method="POST" novalidate>
                    <div class="card-body">
                        <div class="form-group">
                            <label>Username</label>
                            <input type="text" class="form-control" name="username" required="" value="<%= (typeof data !=='undefined' ) ? data.username : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Username
                            </div>
                            <span class="text-danger"><%= (typeof errors !=='undefined' ) ? errors.username : '' %></span>
                        </div>
                        <div class="form-group">
                            <label>Nama User</label>
                            <input type="text" class="form-control" name="nama" required="" value="<%= (typeof data !=='undefined' ) ? data.nama : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Nama User
                            </div>
                        </div>
                        <div class="form-group">
                            <label>Email</label>
                            <input type="email" class="form-control" name="email" required="" value="<%= (typeof data !=='undefined' ) ? data.email : '' %>">
                            <div class="invalid-feedback">
                                Masukkan Email
                            </div>
                        </div>
                    </div>
                    <!-- /.card-body -->
                    <div class="card-footer">
                        <button type="submit" class="btn btn-success">
                            <%= title.split(' ')[0] == 'Tambah' ? 'Simpan' : 'Update' %>
                        </button>
                    </div>
                </form>
            </div>
            <!-- /.card -->
        </div>
        <!--/.col (left) -->
        <!-- right column -->
        <div class="col-md-6">

        </div>
        <!--/.col (right) -->
    </div>